const PDA_DATA = {
    "1": {
        "Expresión de fracciones como decimales y de decimales como fracciones": [
            "Usa diversas estrategias al convertir números fraccionarios a decimales y viceversa"
        ],
        "Extensión de los números a positivos y negativos y su orden": [
            "Reconoce la necesidad de los números negativos a partir de usar cantidades que tienen al cero como referencia",
            "Compara y ordena números con signo (enteros, fracciones y decimales) en la recta numérica y analiza en qué casos se cumple la propiedad de densidad"
        ],
        "Extensión del significado de las operaciones y sus relaciones inversas": [
            "Reconoce el significado de las cuatro operaciones básicas y sus relaciones inversas al resolver problemas que impliquen el uso de números con signo",
            "Comprueba y argumenta si cada una de estas operaciones cumple las propiedades: conmutativa, asociativa y distributiva",
            "Identifica y aplica la jerarquía de operaciones y símbolos de agrupación al realizar cálculos"
        ],
        "Regularidades y Patrones": [
            "Representa algebraicamente una sucesión con progresión aritmética de figuras y números"
        ],
        "Introducción al álgebra": [
            "Interpreta y plantea diversas situaciones del lenguaje común al lenguaje algebraico y viceversa",
            "Representa algebraicamente perímetros de figuras"
        ],
        "Ecuaciones lineales y cuadráticas": [
            "Resuelve ecuaciones de la forma Ax=B, Ax+B=C, Ax+B=Cx+D con el uso de las propiedades de la igualdad",
            "Modela y resuelve problemas cuyo planteamiento es una ecuación lineal",
            "Resuelve problemas de porcentajes en diversas situaciones"
        ],
        "Funciones": [
            "Relaciona e interpreta relaciones proporcional y no proporcional a partir de su representación tabular, gráfica y con diagramas",
            "Modela y resuelve diversas situaciones a través de ecuaciones proporcionales con constante positiva y negativa"
        ],
        "Rectas y ángulos": [
            "Explora las figuras básicas como rectas y ángulos y su notación",
            "Encuentra y calcula los ángulos que se forman al intersecar dos segmentos"
        ],
        "Construcción y propiedades de las figuras planas y cuerpos": [
            "Utiliza la regla y el compás para trazar: punto medio, mediatriz de un segmento, segmentos y ángulos congruentes, bisectriz de un ángulo, rectas perpendiculares y rectas paralelas",
            "Identifica y traza las rectas notables en triángulos y cuadriláteros",
            "Construye y clasifica triángulos y cuadriláteros a partir del análisis de distinta información"
        ],
        "Circunferencia, círculo y esfera": [
            "Identifica y traza las rectas notables en la circunferencia y las relaciones entre ellas",
            "Investiga figuras relacionadas con círculos y propiedades de los círculos",
            "Construye circunferencias a partir de distinta información"
        ],
        "Medición y cálculo en diferentes contextos": [
            "Introduce la idea de distancia entre dos puntos como la longitud del segmento que los une",
            "Encuentra la distancia de un punto a una recta y la distancia entre dos rectas paralelas",
            "Explora la desigualdad del triángulo",
            "Obtiene y aplica fórmulas o usa otras estrategias para calcular el perímetro y el área de polígonos regulares e irregulares y del círculo"
        ],
        "Obtención y representación de información": [
            "Usa tablas, gráficas de barras y circulares para el análisis de información"
        ],
        "Interpretación de la información a través de medidas de tendencia central y de dispersión": [
            "Determina e interpreta la frecuencia absoluta, la frecuencia relativa, la media, la mediana y la moda en un conjunto de datos",
            "Usa e interpreta las medidas de tendencia central (moda, media aritmética y mediana) y el rango de un conjunto de datos, y justifica con base en ellas sus decisiones"
        ],
        "Azar y probabilidad": [
            "Compara cualitativamente dos o más eventos a partir de sus resultados posibles, usa relaciones como: 'es más probable que...', 'es menos probable que...'",
            "Identifica eventos en los que interviene el azar, determina el espacio muestral y experimenta",
            "Identifica diversos procedimientos de conteo y resuelve problemas"
        ]
    },
    "2": {
        "Extensión del significado de las operaciones y sus relaciones inversas": [
            "Usa criterios de divisibilidad y números primos al resolver problemas que implican calcular el máximo común divisor y el mínimo común múltiplo",
            "Calcula potencias con exponente entero y la raíz cuadrada. Usa la notación científica"
        ],
        "Regularidades y Patrones": [
            "Representa algebraicamente una sucesión con progresión cuadrática de figuras y números"
        ],
        "Introducción al álgebra": [
            "Representa algebraicamente áreas que generan una expresión cuadrática",
            "Identifica y usa las propiedades de los exponentes al resolver distintas operaciones algebraicas"
        ],
        "Ecuaciones lineales y cuadráticas": [
            "Resuelve desigualdades con expresiones algebraicas",
            "Modela y soluciona sistemas de dos ecuaciones lineales con dos incógnitas por algún método para dar respuesta a un problema"
        ],
        "Funciones": [
            "Relaciona e interpreta la proporcionalidad inversa de dos magnitudes o cantidades, además usa una tabla, gráfica o representación algebraica en diversos contextos"
        ],
        "Rectas y ángulos": [
            "Identifica y usa las relaciones entre los ángulos, lados y diagonales para construir a escala triángulos, cuadriláteros y polígonos regulares o irregulares"
        ],
        "Construcción y propiedades de las figuras planas y cuerpos": [
            "Construye con regla y compás polígonos regulares con distinta información",
            "Identifica y usa las relaciones entre figuras en la construcción de teselados"
        ],
        "Circunferencia, círculo y esfera": [
            "Determina la medida de ángulos inscritos y centrales, así como de arcos de circunferencia",
            "Explora las intersecciones entre círculos y figuras al calcular perímetros y áreas"
        ],
        "Medición y cálculo en diferentes contextos": [
            "Resuelve problemas que implican conversiones en múltiplos y submúltiplos del metro, litro, kilogramo y de unidades del sistema inglés (yarda, pulgada, galón, onza y libra)",
            "Utiliza estrategias diversas para determinar el perímetro y el área de figuras compuestas"
        ],
        "Obtención y representación de información": [
            "Recolecta, registra, lee y comunica información mediante histogramas, gráficas poligonales y de línea"
        ],
        "Interpretación de la información a través de medidas de tendencia central y de dispersión": [
            "Usa e interpreta las medidas de tendencia central (moda, media aritmética y mediana) y de dispersión (rango y la desviación media) de un conjunto de datos, y justifica con base en ellas sus decisiones",
            "Identifica tendencias en los datos centrándose en sus valores representativos y sus variaciones"
        ],
        "Azar y probabilidad": [
            "Realiza experimentos aleatorios y registra los resultados en una tabla de frecuencia como la transición de la probabilidad frecuencial a la teórica",
            "Analiza las características de la medida de la probabilidad y su equivalencia y representación en números decimales, fraccionarios y porcentajes"
        ]
    },
    "3": {
        "Extensión del significado de las operaciones y sus relaciones inversas": [
            "Usa la notación científica al realizar cálculos con cantidades muy grandes o muy pequeñas"
        ],
        "Introducción al álgebra": [
            "Representa algebraicamente áreas y volúmenes de cuerpos geométricos y calcula el valor de una variable en función de las otras"
        ],
        "Ecuaciones lineales y cuadráticas": [
            "Resuelve ecuaciones de la forma Ax²+Bx+C=0 por factorización y fórmula general",
            "Resuelve problemas cuyo planteamiento es una ecuación cuadrática"
        ],
        "Funciones": [
            "Relaciona e interpreta la variación de dos cantidades a partir de su representación tabular, gráfica y algebraica",
            "Explora diversos procedimientos para resolver problemas de reparto proporcional"
        ],
        "Construcción y propiedades de las figuras planas y cuerpos": [
            "Aplica las propiedades de la congruencia y semejanza de triángulos al construir y resolver problemas",
            "Reconoce las propiedades de los sólidos",
            "Explora la generación de sólidos de revolución a partir de figuras planas",
            "Explora y construye desarrollos planos de diferentes figuras tridimensionales, cilindros, pirámides y conos"
        ],
        "Circunferencia, círculo y esfera": [
            "Explora y construye desarrollos planos de esferas",
            "Indaga la generación de esferas a partir de figuras planas",
            "Encuentra relaciones de volumen de la esfera, el cono y el cilindro",
            "Verifica los criterios de existencia y unicidad de estas figuras"
        ],
        "Medición y cálculo en diferentes contextos": [
            "Usa diferentes estrategias para calcular el volumen de prismas, pirámides y cilindros",
            "Formula, justifica y usa el teorema de Pitágoras al resolver problemas",
            "Resuelve problemas utilizando las razones trigonométricas seno, coseno y tangente"
        ],
        "Obtención y representación de información": [
            "Lee, interpreta y comunica información de cualquier tipo de gráficas"
        ],
        "Interpretación de la información a través de medidas de tendencia central y de dispersión": [
            "Determina y compara las medidas de tendencia central (media, mediana y moda) y de dispersión (rango y desviación media) de dos conjuntos de datos para tomar decisiones"
        ],
        "Azar y probabilidad": [
            "Resuelve problemas donde se analicen las características de eventos complementarios y eventos mutuamente excluyentes e independientes",
            "Resuelve problemas donde se calcule la probabilidad de ocurrencia de dos eventos mutuamente excluyentes y de eventos complementarios (regla de la suma)",
            "Resuelve problemas donde se calcule la probabilidad de ocurrencia de dos eventos independientes (regla del producto)",
            "Indaga las condiciones necesarias para que un juego de azar sea justo, con base en la noción de resultados equiprobables y no equiprobables"
        ]
    }
};

// Data Structure
let appData = {
    password: '',
    schools: [],
    unassignedGroups: [],
    deletedItems: []
};

// Shared global state
let currentSchoolId = null;
let currentGroupId = null;
let currentStudentId = null;
let editingSchoolId = null;
let editingGroupId = null;
let editingStudentId = null;
let currentStudentInterests = [];
let currentStudentFutureCareers = [];
let activeInterestFilters = [];
let currentStudentSearchQuery = '';
let currentBapCategories = [];

function normalizeAppData(data) {
    if (!data || typeof data !== 'object') {
        data = { schools: [], unassignedGroups: [], deletedItems: [] };
    }
    if (!Array.isArray(data.schools)) {
        data.schools = [];
    }
    if (!Array.isArray(data.unassignedGroups)) {
        data.unassignedGroups = [];
    }
    if (!Array.isArray(data.deletedItems)) {
        data.deletedItems = [];
    }

    // Migration of legacy root groups (orphaned/unassigned groups)
    if (Array.isArray(data.groups) && data.groups.length > 0) {
        data.groups.forEach(g => {
            const existsInSchool = data.schools.some(s => s.groups && s.groups.some(sg => sg.id === g.id));
            const existsInUnassigned = data.unassignedGroups.some(ug => ug.id === g.id);
            if (!existsInSchool && !existsInUnassigned) {
                data.unassignedGroups.push(g);
            }
        });
        delete data.groups;
    }

    // Ensure each school has valid structure
    data.schools.forEach(school => {
        if (!school.id) school.id = generateUniqueId();
        if (!Array.isArray(school.groups)) school.groups = [];
        if (!Array.isArray(school.observations)) school.observations = [];
        if (!school.internalContext) school.internalContext = '';
        if (!school.externalContext) school.externalContext = '';
        if (!school.schoolProblems) school.schoolProblems = '';

        school.groups.forEach(g => {
            if (!Array.isArray(g.scheduleSessions)) g.scheduleSessions = [];
        });
    });

    data.unassignedGroups.forEach(g => {
        if (!Array.isArray(g.scheduleSessions)) g.scheduleSessions = [];
    });

    return data;
}

function getAllSchools() {
    return appData.schools || [];
}

function getSchoolById(schoolId) {
    if (!schoolId || !appData.schools) return null;
    return appData.schools.find(s => s.id === schoolId) || null;
}

function getCurrentSchool() {
    if (currentSchoolId) {
        return getSchoolById(currentSchoolId);
    }
    if (currentGroupId) {
        return findSchoolByGroupId(currentGroupId);
    }
    return null;
}

function getAllGroups() {
    const all = [];
    if (Array.isArray(appData.schools)) {
        appData.schools.forEach(s => {
            if (Array.isArray(s.groups)) {
                all.push(...s.groups);
            }
        });
    }
    if (Array.isArray(appData.unassignedGroups)) {
        all.push(...appData.unassignedGroups);
    }
    return all;
}

function findGroupById(groupId) {
    if (!groupId) return null;
    if (Array.isArray(appData.schools)) {
        for (const school of appData.schools) {
            if (Array.isArray(school.groups)) {
                const g = school.groups.find(group => group.id === groupId);
                if (g) return g;
            }
        }
    }
    if (Array.isArray(appData.unassignedGroups)) {
        const g = appData.unassignedGroups.find(group => group.id === groupId);
        if (g) return g;
    }
    return null;
}

function findSchoolByGroupId(groupId) {
    if (!groupId || !Array.isArray(appData.schools)) return null;
    for (const school of appData.schools) {
        if (Array.isArray(school.groups)) {
            if (school.groups.some(g => g.id === groupId)) {
                return school;
            }
        }
    }
    return null;
}

function getCurrentGroup() {
    return findGroupById(currentGroupId);
}

function assignGroupToSchool(groupId, targetSchoolId) {
    const group = findGroupById(groupId);
    if (!group) return false;

    // Remove from previous school or unassigned
    if (Array.isArray(appData.unassignedGroups)) {
        appData.unassignedGroups = appData.unassignedGroups.filter(g => g.id !== groupId);
    }
    if (Array.isArray(appData.schools)) {
        appData.schools.forEach(s => {
            if (Array.isArray(s.groups)) {
                s.groups = s.groups.filter(g => g.id !== groupId);
            }
        });
    }

    if (targetSchoolId) {
        const targetSchool = getSchoolById(targetSchoolId);
        if (targetSchool) {
            if (!Array.isArray(targetSchool.groups)) targetSchool.groups = [];
            targetSchool.groups.push(group);
            currentSchoolId = targetSchoolId;
        } else {
            appData.unassignedGroups.push(group);
        }
    } else {
        appData.unassignedGroups.push(group);
    }

    saveData();
    return true;
}

function filterStudentsList(allStudents) {
    if (!Array.isArray(allStudents)) return [];
    const query = normalizeText(currentStudentSearchQuery || '');
    return allStudents.filter(student => {
        const matchesInterest = !activeInterestFilters || activeInterestFilters.length === 0 ||
            activeInterestFilters.every(filter => student.interests?.includes(filter));
        if (!matchesInterest) return false;

        if (!query) return true;

        const fullNameNorm = normalizeText(student.fullName || '');
        const preferredNameNorm = normalizeText(student.preferredName || '');
        const listNumStr = String(student.listNumber || '');

        return fullNameNorm.includes(query) || preferredNameNorm.includes(query) || listNumStr === query;
    });
}

// BAP Categories
const BAP_CATEGORIES = ['Sensorial', 'Motora', 'Cognitiva', 'Socioemocional', 'Contextual/familiar', 'Lingüística'];

// Sociogram Constants
const CHANNEL_COLORS = { 'Visual': '#3b82f6', 'Auditivo': '#f59e0b', 'Kinestésico': '#10b981', 'No especificado': '#94a3b8', '': '#94a3b8' };
const SOCIOGRAM_EDGE_COLORS = { academic: '#6366f1', social: '#22c55e', complementary: '#f97316' };
const SOCIOGRAM_TITLES = { academic: 'Afinidad académica', social: 'Afinidad social', complementary: 'Estilo de trabajo complementario' };
const SOCIOGRAM_LABELS = {
    academic: '¿Con quién de tu grupo te gustaría hacer equipo para el próximo proyecto?',
    social: '¿Con quién prefieres pasar el tiempo en el receso?',
    complementary: '¿Qué tipo de compañero de equipo te complementa mejor?'
};

// Utility Helpers
function escapeHTML(str) {
    if (str === null || str === undefined) return '';
    return String(str)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
}

function normalizeText(text) {
    if (!text) return '';
    return text.normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase();
}

function generateUniqueId() {
    if (typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function') {
        return crypto.randomUUID();
    }
    return Date.now().toString(36) + Math.random().toString(36).substring(2, 9);
}

function hslToHex(h, s, l) {
    l /= 100;
    const a = s * Math.min(l, 1 - l) / 100;
    const f = n => {
        const k = (n + h / 30) % 12;
        const color = l - a * Math.max(Math.min(k - 3, 9 - k, 1), -1);
        return Math.round(255 * color).toString(16).padStart(2, '0');
    };
    return `#${f(0)}${f(8)}${f(4)}`;
}

function generateRandomStudentColor() {
    // Generate distinct, vibrant hues with balanced saturation and lightness
    const hue = Math.floor(Math.random() * 360);
    const sat = Math.floor(Math.random() * 25) + 65; // 65% - 90%
    const light = Math.floor(Math.random() * 16) + 42; // 42% - 58%
    return hslToHex(hue, sat, light);
}

function formatDateTime(dateObj = new Date()) {
    const d = String(dateObj.getDate()).padStart(2, '0');
    const m = String(dateObj.getMonth() + 1).padStart(2, '0');
    const y = dateObj.getFullYear();
    const hrs = String(dateObj.getHours()).padStart(2, '0');
    const mins = String(dateObj.getMinutes()).padStart(2, '0');
    return `${d}/${m}/${y} ${hrs}:${mins}`;
}

function parseDateString(dateStr) {
    if (!dateStr) return new Date(0);
    if (typeof dateStr !== 'string') return new Date(dateStr);

    const parts = dateStr.trim().split(/\s+/);
    const datePart = parts[0] || '';
    const timePart = parts[1] || '';

    let hours = 0, minutes = 0, seconds = 0;
    if (timePart) {
        const timePts = timePart.split(':');
        if (timePts.length >= 2) {
            hours = parseInt(timePts[0], 10) || 0;
            minutes = parseInt(timePts[1], 10) || 0;
            if (timePts.length >= 3) {
                seconds = parseInt(timePts[2], 10) || 0;
            }
        }
    }

    if (datePart.includes('/')) {
        const pts = datePart.split('/');
        if (pts.length === 3) {
            return new Date(parseInt(pts[2], 10), parseInt(pts[1], 10) - 1, parseInt(pts[0], 10), hours, minutes, seconds);
        }
    } else if (datePart.includes('-')) {
        const pts = datePart.split('-');
        if (pts.length === 3) {
            if (pts[0].length === 4) {
                return new Date(parseInt(pts[0], 10), parseInt(pts[1], 10) - 1, parseInt(pts[2], 10), hours, minutes, seconds);
            } else {
                return new Date(parseInt(pts[2], 10), parseInt(pts[1], 10) - 1, parseInt(pts[0], 10), hours, minutes, seconds);
            }
        }
    }
    const d = new Date(dateStr);
    return isNaN(d.getTime()) ? new Date(0) : d;
}

// Global File System Access & Persistence State
const DB_NAME = 'GestionPedagogicaDB';
const STORE_NAME = 'fileHandles';

var currentFileHandle = null;
var currentFileName = 'gestion-pedagogica-' + new Date().toISOString().split('T')[0] + '.json';
var hasUnsavedChanges = false;
var autoSaveTimeout = null;

try {
    const savedName = localStorage.getItem('lastFileName');
    if (savedName && savedName !== 'Sin título.json' && savedName.endsWith('.json')) {
        currentFileName = savedName;
    }
} catch (e) {
    console.warn('No se pudo leer lastFileName de localStorage:', e);
}

function getDefaultFileName() {
    const now = new Date();
    const year = now.getFullYear();
    const month = String(now.getMonth() + 1).padStart(2, '0');
    const day = String(now.getDate()).padStart(2, '0');
    return `gestion-pedagogica-${year}-${month}-${day}.json`;
}

function openHandleDB() {
    return new Promise((resolve) => {
        try {
            if (!window.indexedDB) {
                resolve(null);
                return;
            }
            const req = indexedDB.open(DB_NAME, 1);
            req.onupgradeneeded = () => {
                try {
                    if (!req.result.objectStoreNames.contains(STORE_NAME)) {
                        req.result.createObjectStore(STORE_NAME);
                    }
                } catch (err) {}
            };
            req.onsuccess = () => resolve(req.result);
            req.onerror = () => resolve(null);
        } catch (err) {
            resolve(null);
        }
    });
}

async function setStoredHandle(key, handle) {
    try {
        const db = await openHandleDB();
        if (!db) return false;
        const tx = db.transaction(STORE_NAME, 'readwrite');
        tx.objectStore(STORE_NAME).put(handle, key);
        return new Promise(resolve => {
            tx.oncomplete = () => resolve(true);
            tx.onerror = () => resolve(false);
        });
    } catch (e) {
        return false;
    }
}

async function getStoredHandle(key) {
    try {
        const db = await openHandleDB();
        if (!db) return null;
        const tx = db.transaction(STORE_NAME, 'readonly');
        const req = tx.objectStore(STORE_NAME).get(key);
        return new Promise(resolve => {
            req.onsuccess = () => resolve(req.result || null);
            req.onerror = () => resolve(null);
        });
    } catch (e) {
        return null;
    }
}

async function restoreStoredHandle() {
    try {
        const handle = await getStoredHandle('activeFileHandle');
        if (handle && handle.name) {
            currentFileHandle = handle;
            currentFileName = handle.name;
            try { localStorage.setItem('lastFileName', handle.name); } catch (e) {}
            updateWindowTitle();
        }
    } catch (e) {}
}

function loadData() {
    // Attempt to recover local storage data if present
    try {
        const saved = localStorage.getItem('pedagogicalData');
        if (saved) {
            if (typeof isEncryptedPayload === 'function' && isEncryptedPayload(saved)) {
                // Encrypted with Web Crypto: will be decrypted during login() with activeSessionPassword
                restoreStoredHandle();
                return;
            }
            const parsed = JSON.parse(saved);
            if (parsed && typeof parsed === 'object') {
                appData = normalizeAppData(parsed);
                hasUnsavedChanges = false;
                updateWindowTitle();
            }
        }
    } catch (e) {
        console.error("Error al cargar datos de localStorage:", e);
    }
    restoreStoredHandle();
}

async function saveData() {
    hasUnsavedChanges = true;
    updateWindowTitle();

    // Maintain secure encrypted localStorage backup to prevent data exposure
    try {
        if (typeof activeSessionPassword !== 'undefined' && activeSessionPassword && typeof encryptData === 'function') {
            const encryptedPayload = await encryptData(appData, activeSessionPassword);
            localStorage.setItem('pedagogicalData', JSON.stringify(encryptedPayload));
        } else {
            localStorage.setItem('pedagogicalData', JSON.stringify(appData));
        }
    } catch (e) {
        console.warn("No se pudo escribir en localStorage cifrado:", e);
    }

    // Auto-save if we already have a file handle
    if (currentFileHandle) {
        clearTimeout(autoSaveTimeout);
        autoSaveTimeout = setTimeout(() => {
            saveToFile(currentFileHandle);
        }, 1000); // 1s debounce
    }
}

async function saveToFile(handle) {
    if (!handle) return false;
    try {
        // Query / Request write permission if required
        if (typeof handle.queryPermission === 'function') {
            let permission = await handle.queryPermission({ mode: 'readwrite' });
            if (permission !== 'granted' && typeof handle.requestPermission === 'function') {
                permission = await handle.requestPermission({ mode: 'readwrite' });
            }
            if (permission !== 'granted') {
                return false;
            }
        }

        const writable = await handle.createWritable();
        await writable.write(JSON.stringify(appData, null, 2));
        await writable.close();
        hasUnsavedChanges = false;
        currentFileHandle = handle;
        if (handle.name) {
            currentFileName = handle.name;
            localStorage.setItem('lastFileName', handle.name);
        }
        await setStoredHandle('activeFileHandle', handle);
        updateWindowTitle();
        return true;
    } catch (err) {
        console.error("Error al guardar en archivo:", err);
        return false;
    }
}

function updateWindowTitle() {
    const fileName = (currentFileHandle && currentFileHandle.name) || currentFileName || getDefaultFileName();
    const indicator = hasUnsavedChanges ? '*' : '';
    document.title = `${indicator}${fileName} - Sistema de Gestión Pedagógica`;
}

function getStudentShortLabel(student) {
    if (!student) return '';
    if (student.preferredName && student.preferredName.trim()) {
        return student.preferredName.trim();
    }
    const parts = (student.fullName || '').trim().split(/\s+/).filter(p => p.length > 0);
    if (parts.length >= 3) {
        // Formato escolar latinoamericano: [Apellido Paterno] [Apellido Materno] [Nombre 1] [Nombre 2...]
        // Devuelve "Nombre ApellidoPaterno", p.ej. "Vaneli Perez"
        return `${parts[2]} ${parts[0]}`;
    } else if (parts.length === 2) {
        return `${parts[1]} ${parts[0]}`;
    }
    return parts[0] || student.fullName || '';
}

function getMentionedStudents(students, text) {
    if (!students || !students.length || !text) return [];

    let normWorkingText = normalizeText(text);
    const matchedStudents = [];
    const matchedIds = new Set();

    function addMatch(s) {
        if (!s || matchedIds.has(s.id)) return;
        matchedIds.add(s.id);
        matchedStudents.push(s);
    }

    function maskText(targetNorm) {
        if (!targetNorm || targetNorm.length < 2) return;
        while (normWorkingText.includes(targetNorm)) {
            const idx = normWorkingText.indexOf(targetNorm);
            normWorkingText = normWorkingText.substring(0, idx) + ' '.repeat(targetNorm.length) + normWorkingText.substring(idx + targetNorm.length);
        }
    }

    // Ordenar alumnos por longitud de nombre completo descendente para priorizar coincidencias exactas y largas
    const studentsSortedByLength = [...students].sort((a, b) => (b.fullName || '').length - (a.fullName || '').length);

    // --- FASE 1: Menciones explícitas con @ (Máxima prioridad y aislamiento) ---
    // 1.1: @NombreCompleto (ej. @ROMO SOTO CALEB UZZIEL JESUS)
    studentsSortedByLength.forEach(s => {
        if (!s.fullName) return;
        const atFullName = normalizeText('@' + s.fullName);
        if (normWorkingText.includes(atFullName)) {
            addMatch(s);
            maskText(atFullName);
        }
    });

    // 1.2: @NombrePreferido (ej. @Caleb o @Vaneli)
    studentsSortedByLength.forEach(s => {
        if (s.preferredName && s.preferredName.trim().length >= 2) {
            const atPref = normalizeText('@' + s.preferredName.trim());
            if (normWorkingText.includes(atPref)) {
                addMatch(s);
                maskText(atPref);
            }
        }
    });

    // 1.3: @NombreCorto (ej. @Caleb Romo)
    studentsSortedByLength.forEach(s => {
        const shortLabel = getStudentShortLabel(s);
        if (shortLabel && shortLabel.length >= 4) {
            const atShort = normalizeText('@' + shortLabel);
            if (normWorkingText.includes(atShort)) {
                addMatch(s);
                maskText(atShort);
            }
        }
    });

    // --- FASE 2: Nombre Completo exacto en el texto libre restante ---
    studentsSortedByLength.forEach(s => {
        if (!s.fullName || s.fullName.length < 4) return;
        const normFullName = normalizeText(s.fullName);
        if (normWorkingText.includes(normFullName)) {
            addMatch(s);
            maskText(normFullName);
        }
    });

    // --- FASE 3: Nombre preferido o Nombre de pila en texto libre restante ---
    students.forEach(s => {
        if (matchedIds.has(s.id)) return;

        // 3.1 Nombre preferido como palabra completa (ej. "Vaneli", "Ismael")
        if (s.preferredName && s.preferredName.trim().length >= 3) {
            const normPref = normalizeText(s.preferredName.trim());
            const escaped = normPref.replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&');
            const pattern = new RegExp(`(?:^|[^a-z0-9])${escaped}(?:$|[^a-z0-9])`, 'i');
            if (pattern.test(normWorkingText)) {
                addMatch(s);
                maskText(normPref);
                return;
            }
        }

        const parts = (s.fullName || '').trim().split(/\s+/).filter(p => p.length > 0);
        let surnames = [];
        let givenNames = [];
        if (parts.length >= 3) {
            surnames = [parts[0], parts[1]];
            givenNames = parts.slice(2);
        } else if (parts.length === 2) {
            surnames = [parts[0]];
            givenNames = [parts[1]];
        } else {
            givenNames = parts;
        }

        // 3.2 Nombre + Apellido en texto (ej. "Caleb Romo", "Vaneli Perez", "Alejandra Diaz")
        if (givenNames.length > 0 && surnames.length > 0) {
            for (const gn of givenNames) {
                for (const sn of surnames) {
                    const normCombo1 = normalizeText(`${gn} ${sn}`);
                    const normCombo2 = normalizeText(`${sn} ${gn}`);
                    if (normCombo1.length >= 6 && (normWorkingText.includes(normCombo1) || normWorkingText.includes(normCombo2))) {
                        addMatch(s);
                        maskText(normCombo1);
                        maskText(normCombo2);
                        return;
                    }
                }
            }
        }

        // 3.3 Ambos apellidos juntos (ej. "Romo Soto", "Diaz Olivarez")
        if (surnames.length >= 2) {
            const normSurnames = normalizeText(`${surnames[0]} ${surnames[1]}`);
            if (normSurnames.length >= 6 && normWorkingText.includes(normSurnames)) {
                addMatch(s);
                maskText(normSurnames);
                return;
            }
        }

        // 3.4 Primer nombre de pila principal (ej. "Caleb" en "Caleb Uzziel Jesús")
        // SOLO si no se usaron menciones @ en el texto original y el primer nombre es único en el grupo
        const hasAtInOriginalText = text.includes('@');
        if (!hasAtInOriginalText && givenNames.length > 0) {
            const primaryGivenName = givenNames[0];
            const normPgn = normalizeText(primaryGivenName);
            if (normPgn.length >= 4) {
                const countWithSamePrimary = students.filter(other => {
                    const oParts = (other.fullName || '').trim().split(/\s+/).filter(p => p.length > 0);
                    const oGn = oParts.length >= 3 ? oParts.slice(2) : (oParts.length === 2 ? [oParts[1]] : oParts);
                    return oGn.length > 0 && normalizeText(oGn[0]) === normPgn;
                }).length;

                if (countWithSamePrimary === 1) {
                    const escaped = normPgn.replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&');
                    const pattern = new RegExp(`(?:^|[^a-z0-9])${escaped}(?:$|[^a-z0-9])`, 'i');
                    if (pattern.test(normWorkingText)) {
                        addMatch(s);
                        maskText(normPgn);
                        return;
                    }
                }
            }
        }
    });

    return matchedStudents;
}

function isStudentMentionedInText(student, text, studentsContext) {
    if (!student || !text) return false;
    const list = (studentsContext && studentsContext.length) ? studentsContext : [student];
    const mentioned = getMentionedStudents(list, text);
    return mentioned.some(s => s.id === student.id);
}

function getPairRelationshipScore(group, id1, id2, students) {
    if (!group || !group.observations) return 0;
    const pairId = id1 < id2 ? `${id1}_${id2}` : `${id2}_${id1}`;
    
    let score = 0;
    
    const conflictKeywords = ['ignor', 'rechaz', 'pelea', 'discut', 'conflict', 'problem', 'excluy', 'molest', 'enoj', 'tensión', 'tension', 'distanc', 'discrepan'];
    const supportKeywords = ['ayud', 'apoy', 'colabor', 'compañer', 'integr', 'defend', 'resolv', 'solidar', 'felicit', 'destac', 'participa'];
    
    const studentsList = students || group.students || [];
    const s1 = studentsList.find(s => s.id === id1);
    const s2 = studentsList.find(s => s.id === id2);
    if (!s1 || !s2) return 0;

    group.observations.forEach(obs => {
        const textLower = normalizeText(obs.text);
        const hasConflictKw = conflictKeywords.some(kw => textLower.includes(normalizeText(kw)));
        const hasSupportKw = supportKeywords.some(kw => textLower.includes(normalizeText(kw)));
        
        const mentioned = getMentionedStudents(studentsList, obs.text);
        const is1 = mentioned.some(s => s.id === id1);
        const is2 = mentioned.some(s => s.id === id2);

        if (is1 && is2) {
            let isConflict = false;
            if (obs.confirmedConflicts !== undefined) {
                isConflict = obs.confirmedConflicts.includes(pairId);
            } else {
                isConflict = hasConflictKw;
            }

            let isSupport = false;
            if (obs.confirmedSupports !== undefined) {
                isSupport = obs.confirmedSupports.includes(pairId);
            } else {
                isSupport = hasSupportKw && !hasConflictKw;
            }
            
            if (isConflict) {
                score -= 5.0;
            }
            if (isSupport) {
                score += 2.0;
            }
        }
    });
    
    return score;
}

